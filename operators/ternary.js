// Conditional (Ternary) Operator
// Syntax: variable = condition ? value1 : value2;
let age = 16;
let message = age > 18 ? "old" : "young";

let isOnline = true;
let user = isOnline ? "Orel Hassid" : null;

console.log(message); // young
console.log(user); // Orel Hassid
