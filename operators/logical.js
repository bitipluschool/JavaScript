let age = 20;
let gender = "male";

// Logical AND (&&)
console.log(age > 18 && gender === "male");

// Logical OR (||)
console.log(age < 18 || gender === "male");

// Logical NOT (!==)
let toggle = true;
console.log(!toggle);
