function getUser() {
  return {
    name: "Orel Hassid",
    age: 23,
  };
}

const user = getUser();

function addition(x, y) {
  console.log(x + y);
}

addition(10, 5);

function print(message) {
  console.log(message);
}

// print("שלום");
// print("סבבה");

// פרק 5 - פונקציות
// Syntax: function <name>() {...}
