/*  ---  JavaScript Basic - Functions  ---  */
// Syntax: function <name>() {...}

function getUser() {
  return {
    name: "Orel Hassid",
    age: 23,
  };
}
// בלובלבובבו
const user = getUser();

function addition(x, y) {
  console.log(x + y);
}

addition(10, 5);

function print(message) {
  console.log(message);
}
