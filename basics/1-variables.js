// פרק 1 - משתנים
// var, let, const

let firstName; // הצהרה (Declration)
firstName = "Orel"; // הקצאה (Assignment)

// פרק 2 - סוגי נתונים
// פשוטים (Primitives)
let name = "Orel Hassid";
let age = 23; // אותיות (String)
let isOnline = true; // אמת או שקר (Boolean)
let address = undefined; // לא מוגדר (undefined)
let data = null; // ריק (null)
